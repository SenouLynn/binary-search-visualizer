Where I left off:

Trying to set a timer for
- computer guesses a number
- then checks number and updates min/maxex
- then if num is > or < than target, num is initially pushed to mid arr, then every other number gets pushed to submin or super max arr
- then create divs